# FlowState runtime — copy to .env for local overrides (.env is git-ignored; never commit it)
# FLOWSTATE_ENVIRONMENT=local
# FLOWSTATE_LOG_LEVEL=INFO
# FLOWSTATE_AGENT_MODEL=sonnet
# FLOWSTATE_WORKSPACE_DIR=./.data
# ANTHROPIC_API_KEY=sk-ant-...   # optional locally if you're logged into Claude Code
